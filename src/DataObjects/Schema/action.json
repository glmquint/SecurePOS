{
    "type": "object",
    "properties": {
        "action": {
            "type": "string",
            "enum": [
                "confirm",
                "Confirm",
                "reject",
                "Reject"
            ]
        }
    },
    "required": ["action"]
}
